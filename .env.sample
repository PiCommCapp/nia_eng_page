# =============================================================================
# N8N AUTOMATION PLATFORM ENVIRONMENT CONFIGURATION
# =============================================================================
# This file contains all the environment variables required for the N8N
# automation platform Docker Compose setup.
#
# Instructions:
# 1. Copy this file to .env: cp .env.sample .env
# 2. Update the values below with your actual configuration
# 3. Never commit the .env file to version control (it's in .gitignore)
# =============================================================================

# =============================================================================
# DOMAIN CONFIGURATION
# =============================================================================
# Your domain name for N8N access (e.g., example.com)
# This will be used for N8N_HOST and WEBHOOK_URL
DOMAIN_NAME=your-domain.com

# =============================================================================
# TIMEZONE CONFIGURATION
# =============================================================================
# Timezone for the N8N platform (e.g., America/New_York, Europe/London, UTC)
# See: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=UTC

# =============================================================================
# N8N SERVICE CONFIGURATION
# =============================================================================
# Port on the host machine to expose N8N (default: 5678)
# Make sure this port is available and not used by other services
N8N_PORT=5678

# =============================================================================
# POSTGRESQL DATABASE CONFIGURATION
# =============================================================================
# Database name for N8N data
N8N_POSTGRES_DB=n8n

# Database username for N8N
N8N_POSTGRES_USER=n8n

# Database password for N8N (CHANGE THIS TO A SECURE PASSWORD!)
# Generate a strong password: openssl rand -base64 32
N8N_POSTGRES_PASSWORD=your-secure-database-password-here

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. Change all default passwords before deploying to production
# 2. Use strong, unique passwords for database access
# 3. Consider using environment-specific configurations
# 4. Regularly rotate passwords in production environments
# 5. Monitor access logs for any unauthorized access attempts

# =============================================================================
# OPTIONAL CONFIGURATIONS
# =============================================================================
# Uncomment and modify these if you need additional N8N configurations:

# N8N Encryption Key (for encrypting sensitive data)
# Generate with: openssl rand -base64 32
# N8N_ENCRYPTION_KEY=your-encryption-key-here

# N8N User Management (if using external authentication)
# N8N_USER_MANAGEMENT_DISABLED=false
# N8N_USER_MANAGEMENT_JWT_SECRET=your-jwt-secret-here

# N8N Webhook Configuration
# N8N_WEBHOOK_URL=https://n8n.${DOMAIN_NAME}/

# N8N Logging Configuration
# N8N_LOG_LEVEL=info
# N8N_LOG_OUTPUT=console,file
# N8N_LOG_FILE_LOCATION=/home/node/.n8n/logs/n8n.log

# N8N Execution Configuration
# N8N_EXECUTION_MODE=regular
# N8N_QUEUE_BULL_REDIS_HOST=redis
# N8N_QUEUE_BULL_REDIS_PORT=6379
# N8N_QUEUE_BULL_REDIS_PASSWORD=your-redis-password

# =============================================================================
# DOCKER COMPOSE NOTES
# =============================================================================
# This .env file is used by Docker Compose to substitute variables in
# the compose.yml file. All variables referenced with ${VARIABLE_NAME}
# in compose.yml must be defined in this file.
#
# To start the services:
#   docker-compose up -d
#
# To view logs:
#   docker-compose logs -f
#
# To stop the services:
#   docker-compose down
#
# To restart a specific service:
#   docker-compose restart n8n
